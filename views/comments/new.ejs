<% include ../partials/header%>


    <div class="row">

<h1 style="text-align: center; margin-top: 20px">Add New Comment to <%= campground.name %></h1>
    <div style="width: 30%; margin: 45px auto">
            <form action="/campgrounds/<%= campground._id %>/comments" method="POST"> <!-- see here post req is routing to /campgrounds and then again redirect to /campgrounds as a get request which is a diff page-->
                <div class="form-group">
                    <input class="form-control" type="text" name="comment[text]"  placeholder="text"></input>
                </div>             
                <div class="form-group">
                        <button class="btn btn-lg btn-default btn-block btn-primary">Submit</button>   
                </div>
            </form>
            <a href="/campgrounds/<%= campground._id %>">Go back</a> <!-- see here u gotta specify thr route relative to home page and not the file name...MAADARCHOD-->
    </div>



    </div>

<% include ../partials/footer%>
 